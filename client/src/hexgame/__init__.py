"""Hex game clients: random / model-driven / pygame GUI.

Connects to a hexgame-server arena (the separate ``hexgame-server`` distribution
on PyPI). Console entry point: ``hexgame {random,play,gui}``.
"""

__version__ = "0.3.0"
